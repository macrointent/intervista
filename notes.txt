SETUP
    python3 -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt

RUN Locally
    uvicorn intervista:app --host 0.0.0.0 --port 8002 --reload

DEPLOY
    docker build -t intervista-mcp . 
    docker run --env-file .env -p 8002:8002 intervista-mcp
